//Literals

//JS defines two typesof values: Fixed values and and Variable Values
//Fixed Variables = Literals (hardcoded)
//Variable Values = Variables

//All literal examples below

let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday']
console.log(days.toString());  //method = parentheses, also defined as 
console.log(days.length);   // property

let tired = true;

let soup = {
    a: 'chicken noodle',
    b: 'tomato',
    c: 'beef and barley',
}
console.log(soup.a);


let car = 'Honda';   

let dec = 10.2         
